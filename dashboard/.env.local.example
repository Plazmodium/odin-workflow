# Odin Dashboard - Supabase Configuration
# Copy this file to .env.local and fill in your Supabase credentials.
# IMPORTANT: Never commit .env.local to version control.

# Your Supabase project URL (server-side)
SUPABASE_URL=https://your-project.supabase.co

# Your Supabase project URL (client-side, for public storage access)
# This is the same URL as SUPABASE_URL but with NEXT_PUBLIC_ prefix
# so it can be accessed in client components (e.g., for fetching archived files)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase Secret Key (bypasses RLS — server-side only)
# Find this in: Supabase Dashboard → Settings → API → Secret keys
# NOTE: The "Publishable key" will NOT work — you need the "Secret key"
# (Previously called "Service Role Key" — Supabase renamed this)
SUPABASE_SECRET_KEY=your-secret-key-here
